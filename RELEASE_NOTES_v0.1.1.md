# IndexTab-Win v0.1.1 — 첫 동작 버전 (Portable)

각 모니터 가장자리에 **세로 인덱스 탭 스트립**을 띄워, 그 모니터에 창을 가진 앱을
한눈에 보고 **클릭 한 번으로 전환**하는 유틸리티입니다.

> 🍎🪟 macOS 원본 **[IndexTab-Mac](https://github.com/blackcore76/IndexTab-Mac)** 의 **형제 앱** — Windows 네이티브(C# WPF) 버전입니다.
> 두 앱은 별도 저장소에서 각각 개발되며, 버전 트랙은 독립적입니다. (macOS는 v0.1.x 계열을 별도로 진행)

---

## ✨ 이번 버전에서 되는 것

- 🖥️ **트레이 상주** — 창 없이 트레이 아이콘으로 동작 (우클릭 메뉴로 설정/종료)
- 📑 **세로 인덱스 탭 스트립** — 모니터 가장자리에 항상-위 투명 스트립
  - 한글은 정방향(upright), 영문은 회전 렌더 · 그라데이션/seam 그림자
- 🖱️ **클릭 전환** — 탭 클릭 시 해당 창을 앞으로 (최소화된 창도 복원)
- ✋ **호버 확장** — 마우스 올리면 아이콘 + 앱 이름으로 부드럽게 펼침 (0.13s easeOut)
- 🧩 **멀티모니터** — 모니터별 스트립 · 좌/우 위치 선택 · Per-Monitor V2 DPI 대응
- 💾 **설정 저장** — `%APPDATA%\IndexTab\settings.json`

## 📦 설치 / 실행 (포터블)

설치 과정이 없습니다. 압축을 풀고 **`IndexTab.exe` 실행**하면 트레이에 상주합니다.

- **요구 사항:** Windows 10 / 11 (64-bit)
- 종료: 트레이 아이콘 우클릭 → 종료
- 시작 프로그램 등록을 원하면 `IndexTab.exe` 바로가기를 `shell:startup` 폴더에 넣으세요.

## 🔐 권한 안내 — 두 가지 실행 모드 (중요)

Windows 보안 정책(UIPI)상, 낮은 권한 앱은 **관리자 권한으로 띄운 창**을 제어할 수 없습니다.
그래서 상황에 맞춰 두 가지로 쓸 수 있습니다:

| 실행 방식 | 제어 범위 |
|---|---|
| **그냥 실행** | 일반 앱 창 전환 (탐색기·브라우저·메신저 등 — 대부분 이걸로 충분) |
| **관리자 권한으로 실행** (우클릭 → *관리자 권한으로 실행*) | 위 + **관리자로 띄운 창**(관리자 PowerShell/터미널 등)까지 제어 |

> 관리자로 띄운 창까지 전환하고 싶을 때만 IndexTab을 관리자 권한으로 실행하면 됩니다.
> (향후 버전에서 코드 서명 + uiAccess 방식으로 UAC 없이 이를 지원하는 것을 검토 중입니다.)

## 🚧 알려진 제한 / 다음 다듬을 점

- 영문 세로 이름이 길면 폰트 축소 대신 말줄임(`…`) 처리됨 → macOS처럼 축소-피팅으로 개선 예정
- 혼합 DPI(모니터별 배율 상이) 환경 정밀 검증 필요
- 앱별 색상 팔레트(macOS `ColorPalette`) 도입 여부 검토
- 시작 프로그램 등록 UI(선택)

---

## 🛠️ 이 릴리즈 바이너리 만드는 법 (메인테이너용)

포터블 단일 실행 파일(.NET 런타임 설치 불필요):

```
cd src
dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true
```

산출된 `IndexTab.exe` 를 zip으로 묶어 릴리즈 자산으로 첨부합니다.

**Full Changelog**: 첫 릴리즈
